@using WebAppCore
@model Models.BO.User

<div class="panel panel-default">
    <div class="panel-heading">
        <h3>@Model.FirstName @Model.LastName</h3>
    </div>
    <div class="panel-body">
        <div><b>Client:</b> @Model.Client</div>&nbsp;
        <div><b>Project:</b> @Model.ProjectName</div>&nbsp;
        <div><b>Department:</b> @Model.Department</div>&nbsp;
    </div>
    <div class="panel-footer">
        @*<a class="btn btn-lg"
           asp-page="./Details"
           asp-route-userId="@Model.Id">
            <i class="glyphicon glyphicon-zoom-in"></i>
        </a>*@
        <a class="btn btn-lg" title="Edit"
           asp-page="./Edit"
           asp-route-userId="@Model.Id">
            <i class="glyphicon glyphicon-edit"></i>
        </a>
        @*<a class="btn btn-lg"
           asp-page="./Delete"
           asp-route-userId="@Model.Id">
            <i class="glyphicon glyphicon-trash"></i>
        </a>*@
    </div>

</div>